/// @description DEFAULT_SCALE();
/// @function DEFAULT_SCALE

// Can modify this to something similar to ScaleX/ScaleY 
/// xscale = Step(xscale, scale, argument0); (argument0 is any shift value)

xscale = scale;
yscale = scale;
